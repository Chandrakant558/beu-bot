services:
  - type: worker
    name: beu-gradecard-bot
    runtime: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
